<project>

    <target name="clean">
        <delete dir="build"/>
    </target>

    <target name="compile">
        <mkdir dir="build/classes"/>
        <javac srcdir="." destdir="build/classes"/>
    </target>

    <target name="jar">
        <mkdir dir="build/jar"/>
        <jar destfile="build/jar/test_HTTPS_server.jar" basedir="build/classes">
            <manifest>
                <attribute name="Main-Class" value="test_HTTPS_server"/>
            </manifest>
        </jar>
    </target>

    <target name="run">
        <java jar="build/jar/test_HTTPS_server.jar" fork="true"/>
    </target>

</project>
